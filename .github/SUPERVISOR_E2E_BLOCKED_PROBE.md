# Deliberately unauthorized protected-path probe

This non-executable disposable file exists only to prove that an unapproved `.github/**` change is stopped, reported once, and closed automatically without reaching `main`.
