name: Repair PR 44 Final Source and Codex Races

on:
  push:
    branches: [main]
  schedule:
    - cron: "47 * * * *"
  workflow_dispatch:

permissions: {}

concurrency:
  group: repair-pr-44-final-source-codex-races
  cancel-in-progress: false

jobs:
  repair:
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: read
    env:
      TARGET_PR: "44"
      TARGET_BRANCH: fix/issue-36-human-notice-contract
      EXPECTED_HEAD: b5df12e4368131b3be2ec82977bb703b459157b4
      REPAIR_HELPER: .github/scripts/repair-pr44-final-races.py
    steps:
      - name: Load fixed helper from this default-branch commit
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          helper="$RUNNER_TEMP/repair-pr44-final-races.py"
          metadata=$(gh api \
            -H "Accept: application/vnd.github+json" \
            "repos/$GITHUB_REPOSITORY/contents/$REPAIR_HELPER?ref=$GITHUB_SHA")
          encoding=$(printf '%s' "$metadata" | jq -r .encoding)
          if [ "$encoding" != "base64" ]; then
            echo "Unexpected helper encoding: $encoding" >&2
            exit 1
          fi
          printf '%s' "$metadata" \
            | jq -r .content \
            | tr -d '\n' \
            | base64 --decode > "$helper"
          python3 -m py_compile "$helper"
          python3 "$helper"
