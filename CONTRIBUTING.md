# Contributing

Pull requests from forks are welcome for documentation, tests, and implementation improvements. Fork code receives read-only checks only.

A contribution that changes `.github/**`, `bootstrap/**`, supervisor code, security policy, authentication, permissions, deployment, or data-destructive behavior requires a maintainer-authored Issue with explicit protected-change authorization.

Do not place Secrets, private URLs, private logs, product requirements, or customer data in contributions.
